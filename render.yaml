services:
  - type: web
    name: resumeiq
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: "5000"
      - key: AI_PROVIDER
        value: "none"
